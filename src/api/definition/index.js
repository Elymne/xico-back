const servicesDefinitions = require('./services')
const routesDefinitions = require('./routes')
const controllersDefinitions = require('./controllers')

module.exports = {
    servicesDefinitions,
    routesDefinitions,
    controllersDefinitions,
}
